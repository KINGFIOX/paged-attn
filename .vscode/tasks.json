{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run paged attention naive with uv",
      "type": "shell",
      "command": "uv",
      "args": [
        "run",
        "python",
        "-m",
        "paged_attn.paged_attention_naive"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
